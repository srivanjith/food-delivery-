{
  "name": "food-delivery-app-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix db",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend && npm install --prefix db"
  },
  "dependencies": {
    "mongoose": "^9.7.3"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
